import styled from 'styled-components';

const ButtonStyle = styled.button`
	border: 0;
	padding: 0 0.75rem;
	font-size: 100%;
	&:hover {
		cursor: pointer;
	}
	i {
		font-size: 1.5rem;
	}
`;

export default ButtonStyle;
